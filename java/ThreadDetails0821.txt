1、实现线程安全的方法
	使用同步代码块、同步方法
	使用局部变量，因为局部变量只在方法中有效，一旦方法执行结束之后，该局部变量就被回收。
		所以一个局部变量不可能被几个对象同时使用，所以根本就不存在同步的问题，每当进入这个
		方法的时候，jvm就会创建一组全新的局部变量。
	构造器一般都是线程安全的，在构造函数返回之前，没有任何一个类拥有这个对象的引用，所以
		不可能有两个线程都有这个对象的引用
	将非线程安全的类作为一个线程安全的类的属性，并且