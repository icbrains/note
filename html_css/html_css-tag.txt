HTML中重要的元素：
		--div
		--span
		--p
		div、span、p都可以作为其他元素的容器，span、p标签通常只能包含文本、链接、图片文本格式化元素和表单控件元素，但是span不能包含p元素，p标签可以包含span标签，div可以包含任何标签；
		HTML5保留的标签元素
				--br
			--文本格式化元素
				--b
				--i
				--strong
				--sub
				--sup
				--em
			--语义相关元素
				--abbr
				--blockquote 
					--attribute：cite（URL）
				--bdo（bearing display ）
					--attribute：dir（rtl、ltr）
				--q
				--cite（引用）
				--address
				--pre（preformat）
				--ins
				--del
				--dfn
				--code
				--samp
				--kbd
				--var
			--超链接和锚点
				--a
					attribute：href、target media
				a标签元素可以包含文本图像，各种文本格式化元素和表单元素等内容
				--定义锚点：<a name="test"></a>
				--找到锚点：<a href="index.html#test"></a>{#表示用来指导浏览器的动作，后面的参数表示在浏览器中的位置，对服务器不产生任何影响}
			--列表相关的元素
				--ul
				--ol
					attribute：start、type
					--ol和ul只能包含li子标签
					--li
				--dl
					attribute：只能包含dt和dd
					--dt
					--dd
			--图像相关的
				--img
					attribute：src、alt、height、width。。。
			--表格相关的元素
				--table
				--caption
				--thead
				--tbody
				--tfoot
				--tr
				--td
				--th
			--HTML5新增加的元素
				--contentEditable
				--hidden
				--designMode
				--spellcheck
			--HTML5新增的常用元素
				--article{可以包含header、footer、section、article}
				--section
				--nav
				--aside
				--header
				--hgroup
				--footer
				--figure
				--figcaption
				--mark
				--details
				--summary
			--HTML5新增加的两个特殊功能的元素
				--meter
				--progress
			--表单元素
				--form
					attribute：action、method、enctype、name
					form属性(html5新增加的属性)
					formxxxx属性
					placeholder属性
			--HTML5新增的canvas元素

CSS3的相关知识
	--

